# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# Editor / OS
.vscode/
.idea/
Thumbs.db
.DS_Store

# Raw source dumps under data/external: large original downloads that exceed GitHub's file-size
# limits. The pipeline only reads the normalized versions; provenance is documented in each
# resource's manifest.json.
data/external/*/raw/

# Credentials. The UMLS API key is read from the environment, never from a file.
.env
.env.*

# The UMLS release archive is licensed content.
data/external/umls/
data/external/umls_index/
*.rrf
*.RRF
*umls*.zip
*UMLS*.zip
*UMLS*.tar.gz
**/umls_gold_export/


outputs/umls_cache/
cache/umls/

data/external/umls_cache/dense_candidates.parquet
data/external/umls_cache/tfidf_candidates.parquet

outputs/*
!outputs/anamnesis_entities.parquet
!outputs/anamnesis_entities.meta.json

# Internal documents
STATO.md
GUIDA.*


